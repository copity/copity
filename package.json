{
  "name": "@copity/copity",
  "type": "module",
  "version": "0.0.0",
  "license": "MIT",
  "repository": "copity/copity",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "lint": "eslint . --fix",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "release": "pnpm run build && changelogen --release && git push --follow-tags && pnpm publish"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^2.0.0",
    "@nuxt/devtools": "latest",
    "changelogen": "^0.5.5",
    "eslint": "^8.54.0",
    "nuxt": "^3.8.1",
    "vue": "^3.3.8",
    "vue-router": "^4.2.5"
  }
}
